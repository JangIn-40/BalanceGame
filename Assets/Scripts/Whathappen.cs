using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Whathappen : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //이건 내가 무엇을 해야할지 몰라서 그냥 깃허브잔디채우고 싶어서 넣는 칸이다 앞으론 이러진 말자 이러지 말자
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
